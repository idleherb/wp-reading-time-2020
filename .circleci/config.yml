version: 2.1

jobs:
  e2e_test:
    docker:
      - image: cimg/base:2020.01
    steps:
      - setup_remote_docker
      - checkout
      - run:
          name: copy e2e dir to cypress container and run tests
          command: |
            cd e2e-tests
            docker-compose up --no-start --force-recreate  --build
            docker cp ./ "$(docker-compose ps -q e2e-test)":/e2e
            docker-compose run e2e-test
      - run:
          when: always
          name: debug
          command: |
            docker cp "$(docker-compose ps -q e2e-test)":/e2e e2e-tests
            pwd
            ls -haltr
            ls -haltr e2e-tests
            ls -haltr e2e-tests/cypress
            ls -haltr e2e-tests/cypress/videos
            ls -haltr e2e-tests/cypress/xml
      - store_artifacts:
          path: e2e-tests/cypress/videos
      - store_artifacts:
          path: e2e-tests/cypress/screenshots
      - store_test_results:
          path: e2e-tests/cypress/xml

workflows:
  version: 2
  build_and_test:
    jobs:
      - e2e_test
